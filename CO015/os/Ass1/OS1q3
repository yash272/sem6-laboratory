echo "Enter path : "
read ch
checkIfDirectory() {
    if [ -d "$1" ];
    then
        echo "$1 exists and is a directory."
    else
        echo "$1 is not a directory."
    fi
}
checkIfDirectory "$ch"
